version: "3.9"

services:
  web:
    build: .
    container_name: arvutipood-web
    ports:
      - "80:80"
    volumes:
      - ./src:/var/www/html
      - ./logs/apache:/var/log/apache2
    environment:
      APACHE_LOG_DIR: /var/log/apache2
